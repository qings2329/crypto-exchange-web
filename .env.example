# WalletConnect Cloud projectId（https://cloud.reown.com 免费注册）
# 未配置时 MetaMask 等注入钱包可用，仅 WalletConnect 扫码不可用
VITE_WC_PROJECT_ID=

# 自建行情网关根路径。留空（默认 /api/v1/market）时 K 线/深度/成交/Ticker 走
# 本项目 Go 网关（cmd/gateway 反代 internal/market）；设为空字符串则回退直连 Binance。
VITE_MARKET_BASE=/api/v1/market
