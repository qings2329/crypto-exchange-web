{
  "name": "crypto-exchange-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "npm --prefix server install --no-audit --no-fund && npm --prefix server test && node server/monitor-e2e.mjs",
    "test:server": "npm --prefix server install --no-audit --no-fund && npm --prefix server test",
    "test:e2e": "npm --prefix server install --no-audit --no-fund && node server/monitor-e2e.mjs",
    "test:kline": "npm --prefix server install --no-audit --no-fund && node server/kline-ws.e2e.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "playwright": "^1.62.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
